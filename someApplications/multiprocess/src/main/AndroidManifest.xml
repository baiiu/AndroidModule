<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baiiu.multiprocess">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">
        <activity android:name="com.baiiu.multiprocess.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.baiiu.multiprocess.SecondActivity"
            android:process=":remote"
            />

        <activity
            android:name="com.baiiu.multiprocess.ThirdActivity"
            android:process="com.baiiu.mutiprocess.remote"
            />

        <service
            android:name=".BookManagerService"
            android:process=":remote"/>
    </application>

</manifest>